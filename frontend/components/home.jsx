const React = require('react');

const Home = React.createClass({
  render () {
    return (
      <div id='splash'>
        <h1>Welcome to GenkiCupid!</h1>

      </div>
    );
  }
});

module.exports = Home;
